services:
  nginx:
    ports: !override
      - "80:80"
      - "443:443"
    volumes: !override
      - ./infra/nginx/https.conf:/etc/nginx/conf.d/default.conf:ro
      - ${TLS_CERT_DIR:-./secrets/tls}:/etc/nginx/certs:ro
